# Project Start - 新規プロジェクトセットアップ

このコマンドは、新規プロジェクト用のフォルダ構造を自動的に作成します。

## 実行フロー

1. プロジェクト名を確認
2. 保存先フォルダを確認
3. フォルダとファイルを作成
4. **次のステップ：project-rule-create プロンプトを実行**

---

## Step 1: プロジェクト名の確認

新しいプロジェクトの名前を教えてください。

例：
- 「情報処理エキスパート講座」
- 「AIマーケティング講座」
- 「知的生産管理講座」

---

## Step 2: 保存先の確認

プロジェクトフォルダをどこに作成しますか？

例えば、以下のようなフォルダ構造になります：
```
Outputs/Course/{プロジェクト名}/
```

別の場所にする場合は、相対パスまたは絶対パスで指定してください。

---

## Step 3: フォルダとファイルの作成

以下の構造が作成されます：

```
{保存先}/{プロジェクト名}/
├── .cursor/
│   └── rules/
│       └── {プロジェクト名}.mdc
└── Marketing/
    ├── b1/ （注：bは小文字）
    │   ├── 顧客の現状.md
    │   └── Belief-1.md
    ├── b2/ （注：bは小文字）
    │   ├── 過去に失敗した他の手段.md
    │   └── Belief-2.md
    ├── b3/ （注：bは小文字）
    │   ├── 私のベストな手段.md
    │   └── Belief-3.md
    ├── b4/ （注：bは小文字）
    │   ├── オファー内容.md
    │   └── Belief-4.md
    ├── b5/ （注：bは小文字）
    │   ├── 理想の未来.md
    │   └── Belief-5.md
    └── Marketing - {プロジェクト名}.md
```

**注意**: 
- 各ファイルは空のファイルとして作成されます（`touch`コマンドを使用）
- フォルダ名の`b1`、`b2`、`b3`、`b4`、`b5`の`b`は小文字です

---

## 実行方法

このプロンプトを実行したら、AIが以下の質問をします：

1. 「プロジェクト名は何ですか？」
2. 「保存先フォルダはどこにしますか？」（デフォルト提示）

あなたが答えたら、自動的にフォルダとファイルが作成されます。

---

## 完了後（重要）

プロジェクト構造が作成されたら、**次のステップは必須です**：

**実行方法**：
```
/project-rule-create
```

このプロンプトでは、以下の内容を対話的に決めていきます：
1. プロジェクトの特徴
2. 理想のターゲット
3. 顧客の声・USP
4. 顧客の悩み・痛み
5. サマリー作成

---


## AI 実行手順（内部処理）

### Step 1: プロジェクト名を取得

```
まず、ユーザーに質問します：

「新しいプロジェクトの名前を教えてください。

例：
- 情報処理エキスパート講座
- AIマーケティング講座
- 知的生産管理講座」
```

ユーザーの回答を受け取ったら、次のステップへ。

### Step 2: 保存先フォルダを確認

```
次に、ユーザーに質問します：

「プロジェクトフォルダをどこに作成しますか？

デフォルト：
Outputs/Course/{プロジェクト名}/

このまま進めますか？
別の場所にする場合は、相対パスまたは絶対パスで指定してください。」
```

ユーザーの回答を受け取ったら、次のステップへ。

### Step 3: フォルダとファイルを作成

#### 環境変数の設定

```bash
# ワークスペースルート（Cursorで開いているワークスペース）
WORKSPACE_ROOT="$(pwd)"

# プロジェクト名（ユーザーが入力）
PROJECT_NAME="{ユーザーが入力したプロジェクト名}"

# 保存先（デフォルト：Outputs/Course/{プロジェクト名}/）
# ユーザーが指定した保存先を使用
DEST_DIR="${WORKSPACE_ROOT}/{ユーザー指定の保存先}/{プロジェクト名}"
```

以下の順番で作成します：

#### 1. ベースフォルダを作成
```bash
# .cursor/rules/ フォルダを作成
mkdir -p "${DEST_DIR}/.cursor/rules"

# Marketing フォルダを作成
mkdir -p "${DEST_DIR}/Marketing"
```

#### 2. プロジェクトルールファイルを作成
```bash
# .cursor/rules/{プロジェクト名}.mdc を作成（空ファイル）
touch "${DEST_DIR}/.cursor/rules/${PROJECT_NAME}.mdc"
```

#### 3. Marketingサブフォルダを作成（bは小文字）
```bash
# Marketing/b1
mkdir -p "${DEST_DIR}/Marketing/b1"

# Marketing/b2
mkdir -p "${DEST_DIR}/Marketing/b2"

# Marketing/b3
mkdir -p "${DEST_DIR}/Marketing/b3"

# Marketing/b4
mkdir -p "${DEST_DIR}/Marketing/b4"

# Marketing/b5
mkdir -p "${DEST_DIR}/Marketing/b5"
```

#### 4. Marketingファイルを作成（touchコマンドを使用）

**Marketing/b1**:
```bash
# 顧客の現状.md
touch "${DEST_DIR}/Marketing/b1/顧客の現状.md"

# Belief-1.md
touch "${DEST_DIR}/Marketing/b1/Belief-1.md"
```

**Marketing/b2**:
```bash
# 過去に失敗した他の手段.md
touch "${DEST_DIR}/Marketing/b2/過去に失敗した他の手段.md"

# Belief-2.md
touch "${DEST_DIR}/Marketing/b2/Belief-2.md"
```

**Marketing/b3**:
```bash
# 私のベストな手段.md
touch "${DEST_DIR}/Marketing/b3/私のベストな手段.md"

# Belief-3.md
touch "${DEST_DIR}/Marketing/b3/Belief-3.md"
```

**Marketing/b4**:
```bash
# オファー内容.md
touch "${DEST_DIR}/Marketing/b4/オファー内容.md"

# Belief-4.md
touch "${DEST_DIR}/Marketing/b4/Belief-4.md"
```

**Marketing/b5**:
```bash
# 理想の未来.md
touch "${DEST_DIR}/Marketing/b5/理想の未来.md"

# Belief-5.md
touch "${DEST_DIR}/Marketing/b5/Belief-5.md"
```

**Marketing - {プロジェクト名}.md**:
```bash
# 動的にコンテンツを生成（Wikiリンクのパスを保存先に合わせる）
# ワークスペースルートからの相対パスを計算
RELATIVE_PATH="${DEST_DIR#${WORKSPACE_ROOT}/}"

cat > "${DEST_DIR}/Marketing/Marketing - ${PROJECT_NAME}.md" << 'EOF'
- コンセプト
- 理想の顧客
- b1
	- [[RELATIVE_PATH_PLACEHOLDER/Marketing/b1/顧客の現状]]
	- [[RELATIVE_PATH_PLACEHOLDER/Marketing/b1/Belief-1]]
- b2
	- [[RELATIVE_PATH_PLACEHOLDER/Marketing/b2/過去に失敗した他の手段]]
	- [[RELATIVE_PATH_PLACEHOLDER/Marketing/b2/Belief-2]]
- b3
	- [[RELATIVE_PATH_PLACEHOLDER/Marketing/b3/私のベストな手段]]
	- [[RELATIVE_PATH_PLACEHOLDER/Marketing/b3/Belief-3]]
- b4
	- [[RELATIVE_PATH_PLACEHOLDER/Marketing/b4/オファー内容]]
	- [[RELATIVE_PATH_PLACEHOLDER/Marketing/b4/Belief-4]]
- b5
	- [[RELATIVE_PATH_PLACEHOLDER/Marketing/b5/理想の未来]]
	- [[RELATIVE_PATH_PLACEHOLDER/Marketing/b5/Belief-5]]
EOF

# プレースホルダーを実際のパスに置換
sed -i '' "s|RELATIVE_PATH_PLACEHOLDER|${RELATIVE_PATH}|g" \
  "${DEST_DIR}/Marketing/Marketing - ${PROJECT_NAME}.md"
```

#### 4. 完了メッセージ

```
プロジェクト構造を作成しました！

📁 保存先：
{保存先}/{プロジェクト名}/

✅ 作成されたファイル：
- .cursor/rules/
  - {プロジェクト名}.mdc
- Marketing/b1/
  - 顧客の現状.md
  - Belief-1.md
- Marketing/b2/
  - 過去に失敗した他の手段.md
  - Belief-2.md
- Marketing/b3/
  - 私のベストな手段.md
  - Belief-3.md
- Marketing/b4/
  - オファー内容.md
  - Belief-4.md
- Marketing/b5/
  - 理想の未来.md
  - Belief-5.md
- Marketing/Marketing - {プロジェクト名}.md

---

🔴 **次のステップ（必須）**:

必ず以下のプロンプトを実行してください：

/project-rule-create

このプロンプトを実行することで：
- プロジェクトの商品設計が決まります
- プロジェクトフォルダ内の `.cursor/rules/{プロジェクト名}.mdc` に内容が書き込まれます

実行しますか？
```

---

## 注意事項

- **既存フォルダの上書き**: 同名のプロジェクトフォルダが既に存在する場合は、上書き確認をします
- **パス指定**: 
  - デフォルト：`Outputs/Course/{プロジェクト名}/`（相対パス）
  - カスタム：相対パスまたは絶対パスで指定可能
- **ファイル作成**: 各ファイルは`touch`コマンドで空ファイルとして作成されます
- **必須の次のステップ**: プロジェクト構造作成後、必ず `project-rule-create` プロンプトを実行してください
- **.cursor/rules/ フォルダ**: 各プロジェクト専用の `.cursor/rules/{プロジェクト名}.mdc` ファイルが作成されます
- **フォルダ名の注意**: `b1`、`b2`、`b3`、`b4`、`b5`の`b`は小文字です

---

## Beliefファイルの構造

各Beliefは、以下の2つのファイルで構成されます：

| Belief | ファイル1（背景・状況） | ファイル2（Claim・Proof） |
|--------|---------------------|----------------------|
| **B-1** | 顧客の現状.md | Belief-1.md |
| **B-2** | 過去に失敗した他の手段.md | Belief-2.md |
| **B-3** | 私のベストな手段.md | Belief-3.md |
| **B-4** | オファー内容.md | Belief-4.md |
| **B-5** | 理想の未来.md | Belief-5.md |

この構造により、各Beliefの背景情報とClaimが明確に分離され、管理しやすくなります。
